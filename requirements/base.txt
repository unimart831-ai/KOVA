# Core
Django>=5.1,<5.2
django-environ>=0.11.2
psycopg[binary]>=3.1.18

# Django Extensions
django-allauth>=65.0
PyJWT[crypto]>=2.8.0  # Required by allauth Google OAuth (jwtkit)
django-crispy-forms>=2.3
crispy-tailwind>=1.0.3
django-htmx>=1.19
django-extensions>=3.2

# API (for agent communication)
djangorestframework>=3.15
drf-spectacular>=0.28

# Async Tasks
celery>=5.4
django-celery-beat>=2.7
redis>=5.0

# WebSocket (real-time agent status)
channels>=4.1
channels-redis>=4.2
daphne>=4.1

# Security
django-fernet-fields-v2>=0.9
django-ratelimit>=4.1
django-csp>=4.0
bleach>=6.1

# Static files + HTTP server
whitenoise>=6.7
gunicorn>=22.0

# Storage
django-storages>=1.14
boto3>=1.35

# AI & Agents
langchain>=0.3
langgraph>=0.2
langchain-openai>=0.2
langchain-anthropic>=0.2
tavily-python>=0.5

# Vector support
pgvector>=0.3

# Payments
stripe>=10.0

# Email
resend>=2.0
django-anymail[resend]>=12.0

# HTTP clients
requests>=2.31
httpx>=0.27

# Utilities
Pillow>=10.4
rembg>=2.0.50
onnxruntime>=1.16.0
python-slugify>=8.0
PyYAML>=6.0
reportlab>=4.1

# Calendar intelligence (apps/calendar_intel)
holidays>=0.55
hijri-converter>=2.3

# Error tracking (imported by tasks across multiple apps)
sentry-sdk[django]>=2.13

# PDF Reports
xhtml2pdf>=0.2.11
