from django.apps import AppConfig


class EngageConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.messaging.engage"
    verbose_name = "Legacy (engage) - migrations only"
