from django.apps import AppConfig


class QrAttributionConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.commerce.qr_attribution"
    verbose_name = "Legacy (qr_attribution) - migrations only"
