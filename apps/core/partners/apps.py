from django.apps import AppConfig


class PartnersConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.core.partners"
    verbose_name = "Legacy (partners) - migrations only"
